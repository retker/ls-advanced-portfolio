.nav {
    &--color-white {
      color: #fff;
    }
  }
  
  .nav__list {
    display: flex;
    align-items: center;
  }
  
  .nav__item {
    font-weight: 400;
    margin-right: 60px;
  
    &:last-child {
      margin-right: 0;
    }
  }
 
  .nav__link {
    /* text-decoration: none; */
    white-space: nowrap;
  } 