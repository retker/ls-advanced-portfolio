.nav {
    &--color-white {
      color: #fff;
    }
  }
  
  .nav__list {
    display: flex;
    justify-content: center;
  }
  
  .nav__item {
    // margin-right: 60px;
    margin-right: 8%;
  
    &:last-child {
      margin-right: 0;
    }
  }
  
  .nav__link {
    text-decoration: none;
    white-space: nowrap;
  }