.tags {
    display: flex;
    flex-wrap: wrap;

    @include desktop {
        
    }   
}

.tags__item {
    padding: 13px 20px;
    background: #f1f5f9;
    font-size: 15px;
    font-weight: 600;
    color: rgba($text-color, .7);
    border-radius: 50px;
    margin-right: 10px;
    margin-bottom: 10px;
}